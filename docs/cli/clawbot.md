---
summary: "CLI reference for `openhand clawbot` (legacy alias namespace)"
read_when:
  - You maintain older scripts using `openhand clawbot ...`
  - You need migration guidance to current commands
title: "clawbot"
---

# `openhand clawbot`

Legacy alias namespace kept for backwards compatibility.

Current supported alias:

- `openhand clawbot qr` (same behavior as [`openhand qr`](/cli/qr))

## Migration

Prefer modern top-level commands directly:

- `openhand clawbot qr` -> `openhand qr`
