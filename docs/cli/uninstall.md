---
summary: "CLI reference for `openhand uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `openhand uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
openhand uninstall
openhand uninstall --all --yes
openhand uninstall --dry-run
```
